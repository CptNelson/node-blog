@layout('layouts.app')
 
@section('content')
 

  <!-- Main content -->

  <div>

    @each(post in posts)
        <div>
          <a href="/post/{{ post._id }}">
            <h2>
              {{ post.title }}
            </h2>
          </a>
          <p class="post-meta">Posted by
            <a href="#">{{ post.username }}</a>
            on {{ post.createdAt.toDateString() }}
          </p>
        </div>
        <hr>
      @endeach

  </div>
 
@endsection